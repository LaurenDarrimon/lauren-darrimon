<div class="portfolio-items col-md-5 col-lg-4">

    <a href="{{deployed_url}}" target="_blank">
        <h3> {{title}}</h3>
    </a>

    <a href="{{repo_url}}" target="_blank">
        <h3>Code</h3>
        </a>
    <p> {{description}} </p>

    <a class="thumbnail">
        <img src="{{image_path}}" />
    </a>

</div>